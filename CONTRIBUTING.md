# Contributing

Fork → feature branch → Conventional Commits → PR against `main`. For Rust code, run `cargo fmt` and `cargo clippy`. For MicroPython, run `ruff` if available.

Follows the [Skilluv Community Charter](https://github.com/skilluv-community/community-charter).
